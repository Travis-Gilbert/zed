interface:
  display_name: "Zed"
  short_description: "Repo-specific patterns and workflows for zed"
  default_prompt: "Use the zed repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true